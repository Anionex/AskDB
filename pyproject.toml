[project]
name = "askdb"
version = "1.0.0"
description = "AskDB - 自然语言数据库查询助手，基于 Agno 框架的智能数据库助手"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "AskDB Team" }
]
keywords = ["database", "llm", "ai", "natural-language", "sql", "agno"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    # Agno Framework
    "agno>=0.1.0",
    # Core LLM and AI dependencies
    "openai>=1.0.0",
    "google-genai",  # Agno 框架需要新版 google-genai 而非 google-generativeai
    # Database connectivity and ORM
    "sqlalchemy>=2.0.0",
    "psycopg2-binary>=2.9.0",
    "pymysql>=1.1.0",
    # Vector operations and semantic search
    "numpy>=1.24.0",
    "chromadb>=0.4.0",  # 向量数据库，支持第三方 embedding API
    # Data validation and configuration
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "pydantic[email]",
    # Environment and configuration management
    "python-dotenv>=1.0.0",
    # CLI and terminal UI
    "rich>=13.0.0",
    "click>=8.0.0",
    # Web framework
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    # JWT authentication
    "pyjwt>=2.8.0",
    "python-multipart>=0.0.6",
    # Async utilities
    "nest_asyncio>=1.5.0",
    "aiohttp>=3.8.0",
    # Additional utilities
    "requests>=2.28.0",
    "urllib3>=1.26.0",
    "certifi>=2022.12.7",
    "ddgs",
    "sqlparse>=0.5.5",
    "pyyaml>=6.0.3",
]

[project.optional-dependencies]
# Local embedding - 本地 embedding 模型支持（需要下载模型和 torch，约 2GB+）
# 安装方式: uv sync --extra local-embedding
# 如果不安装此依赖，默认使用第三方 OpenAI 格式 embedding API
local-embedding = [
    "sentence-transformers>=2.2.0",  # 本地 embedding 模型（依赖 torch）
    "scipy>=1.10.0",  # 向量计算
]
# FAISS 向量检索支持
vector = [
    "faiss-cpu>=1.7.0",  # For efficient vector similarity search
]
web = [
    "streamlit>=1.28.0",  # For web UI option
]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]
all = [
    "sentence-transformers>=2.2.0",
    "scipy>=1.10.0",
    "faiss-cpu>=1.7.0",
    "streamlit>=1.28.0",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]

# [project.scripts]
# Command-line interface has been removed, use Web API instead

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]

[tool.hatch.build.targets.wheel]
packages = ["tools", "lib", "dialects", "backend"]

